API_TOKEN=your-api-token

# Sandbox token (dummy data, no charges) used by the test suite.
# Tests prefer SANDBOX_API_TOKEN and fall back to API_TOKEN when unset.
SANDBOX_API_TOKEN=your-sandbox-api-token
